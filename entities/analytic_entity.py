class StocksAnalytics:

    def __init__(self, symbol, current_price, market_cap, relative_volume, volatility_week):
        self.symbol = symbol
        self.current_price = current_price
        self.market_cap = market_cap
        self.relative_volume = relative_volume
        self.volatility_week = volatility_week

